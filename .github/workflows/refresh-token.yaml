name: Refresh token
on:
  schedule:
    - cron: '0 */5 * * *'
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh access token
        run: |
          curl --request POST \
          --url 'localhost:3000/api/refresh'
